<script>
// import { useStore } from 'vuex';
// import { computed, watch } from 'vue';
// import { useNotification } from 'naive-ui';

// const store = useStore();
// const toaster = computed(() => store.getters.getToaster);

// watch(() => {
//   if (toaster.value.show) {
//     showNotification(toaster.value);
//   }
// });

// const showNotification = (notif) => {
//   // Using `createNotification` to display the notification

//   // Show the notification
//   notification['warning']({
//     title: notif.type.toUpperCase(),
//     description: notif.message,
//     duration: 3000,  // Customize the duration if needed
//   });

//   console.log('show toaster....')
//   //   store.commit('hideNotification');
// };

// export default defineComponent({
//   name: 'Message',
//   setup() {
//     const notification = useNotification()
//     const showNotification = (notif) => {
//       // Using `createNotification` to display the notification

//       // Show the notification
//       notification['warning']({
//         title: notif.type.toUpperCase(),
//         description: notif.message,
//         duration: 3000,  // Customize the duration if needed
//       });

//       console.log('show toaster....')
//       //   store.commit('hideNotification');
//     }

//     watch(() => {
//       if (toaster.value.show) {
//         showNotification(toaster.value);
//       }
//     });
//     return {
//       showNotification,
//     }
//   },
// })
</script>

<template>
</template>
